{
  "name": "edusity",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://vishal80022.github.io/Edusity",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint . --ext js,jsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
  
  },
  "devDependencies": {
  
    "gh-pages": "^6.1.1"
  }
}